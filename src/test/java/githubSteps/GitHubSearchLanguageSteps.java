package githubSteps;

import githubPages.GitHubRepositoryViewPage;
import io.cucumber.java8.En;
import githubPages.GitHubHomePage;
import githubPages.GitHubSearchPage;

import static com.codeborne.selenide.Selenide.open;

public class GitHubSearchLanguageSteps implements En {

    public GitHubSearchLanguageSteps() {
/*
        // Step 1:
        Given("User opens github page 2", () -> {
            open("https://github.com/");
        });

        // Step 2:
        Given("User inputs {string} text value in search bar 2", (String text) -> {
            new GitHubHomePage().inputTextInSearchBar(text);
        });

        // Step 3:
        Given("User clicks enter 2", () -> {
            new GitHubHomePage().clickSearchButton();
        });

        // Step 4:
        Given("User selects {string} in programming language list", (String text) -> {
            new GitHubSearchPage().clickSelectedLanguage(text);
        });

        // Step 5:
        Given("User selects first repository 2", () -> {
            new GitHubSearchPage().clickFirstRepository();
        });

        // Step 6:
        Given("Check if repository is displayed 2", () -> {
            new GitHubRepositoryViewPage().checkIfRepositoryOpened();
        });
        */

    }
}
